function togglePanel(){
  var panel= document.getElementById('side-panel')
  if (!panel.___closed){
    emile(panel, 'widith:0px')
    panel.___closed = true;
    
  }
  }else{
  emile(panel, 200px);
  panel._____closed = false 
    }
}